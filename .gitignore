# ── Secrets / local config (절대 커밋 금지) ──
.giosk-secrets.env
*.pem
*.key
*.p12
kubeconfig
admin.conf
**/values-*.local.yaml
**/*.secret.yaml
# 설치별 values(관리자·DB 비밀번호가 들어간다). 서버에만 두고 커밋하지 않는다.
my-values.yaml

# ── Node / frontend ──
node_modules/
frontend/dist/
frontend/test-results/
frontend/playwright-report/
frontend/.vite/

# ── Go / backend ──
backend/bin/
*.test
*.out

# ── Helm ──
*.tgz
charts/**/charts/
charts/**/Chart.lock

# ── 내부 노트 / 작업용 산출물(레포 밖으로 분리됨) ──
Memory.md
.impeccable/
**/.impeccable/

# ── OS / editor ──
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
